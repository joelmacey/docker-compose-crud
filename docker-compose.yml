version: "2"
services:
  app:
    container_name: app
    build: ./app
    ports:
      - "80:80"
    depends_on:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
